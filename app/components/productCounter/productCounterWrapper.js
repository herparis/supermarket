import styled from 'styled-components'

import { COLORS } from '../../constants'
 
const ProductCounterWrapper = styled.h2`
  font-family: sans-serif;
  font-size: 1rem;
  color: ${COLORS.DARK_GREY};
  text-align: center;
`

export default ProductCounterWrapper